apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-samuelsena
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 128Mi
  selector:
    matchLabels:
      namespace: samuelsena
